export default function Stage() {
    return (
        <section className="stage-section" aria-label="Escenario del teatro">
            <div className="stage">
                <span>ESCENARIO</span>
            </div>
        </section>
    );
}